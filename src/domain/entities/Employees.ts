export class Employees {
    name: string;
    email: string;
    isManager: boolean;

    constructor(name: string, email: string,isManager: boolean){
        this.name = name;
        this.email = email;
        this.isManager = isManager;
    };

};